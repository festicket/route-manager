// @flow

export type LogoPropTypes = {
  color: 'white' | 'black' | 'primary',
  hoverable?: boolean,
  size: 'small' | 'big',
};

export type FunctionalPropTypes = {
  color: 'white' | 'grey' | 'primary',
  hoverable?: boolean,
};

export type SocialPropTypes = {
  color: 'white' | 'black' | 'primary',
  hoverable?: boolean,
};

export type TrustCompaniesPropTypes = {
  color: 'white' | 'black' | 'primary',
  hoverable?: boolean,
};

export type PaymentTypePropTypes = {};
