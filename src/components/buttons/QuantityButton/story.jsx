// @flow

import React from 'react';
import { storiesOf } from '@storybook/react';
import QuantityButton from './';

storiesOf('Components / Buttons / QuantityButton', module).add(
  'default',
  () => <QuantityButton />,
);
