import React from 'react';
import { storiesOf } from '@storybook/react';
import ButtonPrimitive from './';

storiesOf('Components / Buttons / ButtonPrimitive', module).add(
  'default',
  () => <ButtonPrimitive />,
);
