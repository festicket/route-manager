import React from 'react';
import { storiesOf } from '@storybook/react';
import StickyButtonContainer from './';

storiesOf('Components / Buttons / StickyButtonContainer', module).add(
  'default',
  () => <StickyButtonContainer />,
);
