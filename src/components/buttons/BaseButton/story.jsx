// @flow

import React from 'react';
import { storiesOf } from '@storybook/react';
import BaseButton from './';

storiesOf('Components / Buttons / BaseButton', module).add('default', () => (
  <BaseButton />
));
