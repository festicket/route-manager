import React from 'react';
import { storiesOf } from '@storybook/react';
import ButtonRound from './';

storiesOf('Components / Buttons / ButtonRound', module).add('default', () => (
  <ButtonRound />
));
