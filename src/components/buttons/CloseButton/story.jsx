// @flow

import React from 'react';
import { storiesOf } from '@storybook/react';
import CloseButton from './';

storiesOf('Components / Buttons / CloseButton', module).add('default', () => (
  <CloseButton />
));
